#include <iostream>
#include <stdlib.h>
#include <math.h>
#include "BinomialDistribution.h"

int BinomialDistribution::binomial(){
    int n,x=0,u,x;
    float f;
    for(int i=0;i<=n-1;i++)
    {
       //u = generate random number in [0,1]
       if(u<p)
           x++;
    }
    return x/float(n);
}
